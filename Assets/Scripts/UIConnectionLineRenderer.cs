using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class UIConnectionLineRenderer : MonoBehaviour {

    LineRenderer line;

    public GameObject[] uiElements;
    public GameObject[] targets;

	// Use this for initialization
	void Start () {
        //line = GetComponent<LineRenderer>();
	}
	
	// Update is called once per frame
	void Update () {
        //line.SetPosition(0, uiElements[0].transform.position);
        //line.SetPosition(1, targets[0].transform.position);

        
    }
}
